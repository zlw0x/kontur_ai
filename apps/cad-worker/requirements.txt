# Pinned exactly. The engine's version is part of what produced a model, and a
# range would make two builds of the same document irreproducible for a reason
# that never appears in the document.
build123d==0.11.1
# The contract's own dependencies, pinned to what the API pins so a document
# that validates on one side of the boundary validates on the other. Two
# validators at different versions is how a document becomes buildable in the
# API and refused in the engine.
pydantic==2.13.1
jsonschema==4.25.1
