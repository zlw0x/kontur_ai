<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <Compile Remove="tests\**\*.cs" />
  </ItemGroup>
  <ItemGroup>
    <!-- The bounds comparison is worth testing directly: it is asymmetric for a
         reason that is easy to get wrong and impossible to see from outside. -->
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleToAttribute">
      <_Parameter1>CadAi.GeometryValidation.Tests</_Parameter1>
    </AssemblyAttribute>
  </ItemGroup>

</Project>
